#The standard deviation of the predictor is one half that of the outcome
#sd(x)=1/2*sd(y)
#sd(y)/sd(x) = 1/0.5
#The correlation between the two variables is .5.
#Cor(y,x) = 0.5
#slop = cor(y,x)*sd(y)/sd(x) 01_03 p12
x <- 0.5*(1/0.5)
x